#pragma once

#include <lvgl.h>

namespace desk_ui {

/** Centered uppercase title on a dim circular disc (Weather/Sports/Radar stubs). */
void screen_stub_lvgl_build(lv_obj_t* parent, const char* title);

}  // namespace desk_ui
